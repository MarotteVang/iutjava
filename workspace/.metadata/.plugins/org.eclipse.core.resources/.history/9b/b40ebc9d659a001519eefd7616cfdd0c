package edu.iut.app;

import java.util.ArrayList;
import java.util.List;

import edu.iut.app.Person.PersonFunction;

public class CriteriaPatternDemo {
	   public static void main(String[] args) {
	      List<Person> persons = new ArrayList<Person>();

	      persons.add(new Person(PersonFunction.NONE, "Robert","Bob", "Robert@Bob.com", "0102030405"));
	      persons.add(new Person(PersonFunction.NONE, "John", "Johnny", "John@Johnny.fr", "0201030405"));
	      persons.add(new Person(PersonFunction.NONE, "Laura", "Fema", "Laura@Fema.us", "0605040302"));
	      persons.add(new Person(PersonFunction.NONE, "Diana", "Ross", "Diana@ross.us","0302010405"));
	      persons.add(new Person(PersonFunction.NONE, "Mike", "Ma", "lol@man.fr", "0203040506"));

	      Criteria date = new CriteriaDate();
	      Criteria perso = new CriteriaPersonne();
	      Criteria classroom = new CriteriaClassRoom();
	      Criteria doc = new CriteriaDoc();

	     /* System.out.println("Males: ");
	      printPersons(date.meetCriteria(persons));

	      System.out.println("\nFemales: ");
	      printPersons(perso.meetCriteria(persons));

	      System.out.println("\nSingle Males: ");
	      printPersons(classroom.meetCriteria(persons));

	      System.out.println("\nSingle Or Females: ");
	      printPersons(doc.meetCriteria(persons));
	   }

	   public static void printPersons(List<Person> persons){
	   
	      for (Person person : persons) {
	         System.out.println("");
	      }*/
	   }      
	}
